{
  "name": "groq-pr-reviewer",
  "version": "1.0.0",
  "description": "AI-powered PR reviewer using Groq + gpt-oss-120b",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build dist/main.js -o dist/bundle --license licenses.txt",
    "all": "npm run build && npm run package",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "groq-sdk": "^0.5.0",
    "minimatch": "^9.0.4",
    "tiktoken": "^1.0.15"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.0"
  }
}
